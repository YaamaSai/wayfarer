Fonts load from Google Fonts (see the @import at the top of assets/css/style.css):
- Newsreader (display serif)
- Hanken Grotesk (UI sans)
- JetBrains Mono (numbers/prices)

To self-host, drop the .woff2 binaries here and replace the @import with @font-face rules.
